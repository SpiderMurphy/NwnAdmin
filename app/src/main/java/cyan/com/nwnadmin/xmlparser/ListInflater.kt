package cyan.com.nwnadmin.xmlparser

import android.content.Context
import android.content.res.XmlResourceParser
import android.view.MenuItem

class ListInflater {
    /*companion object {
        fun inflateMenu(context: Context, resourceXml: Int): List<MenuItem> {
            val menu = listOf<MenuItem>()
            val parser = context.resources.getXml(resourceXml)
            parser.require(XmlResourceParser.START_TAG, null, "menu")
            while (parser.next() != XmlResourceParser.END_TAG) {
                if (parser)
            }
        }

        fun readItem(parser: XmlResourceParser): MenuItem {
            val menuItem: MenuItem
            parser.require(XmlResourceParser.START_TAG, null, "item")
            while(parser.next() != XmlResourceParser.END_TAG) {

            }
        }
    }*/
}